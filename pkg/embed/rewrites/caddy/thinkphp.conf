# Deny access to the runtime and application directories
@deny path_regexp deny (?i)(runtime|application)/
respond @deny 403
# Route everything that isn't a real file or directory to index.php
try_files {path} {path}/ /index.php?s={path}&{query}
