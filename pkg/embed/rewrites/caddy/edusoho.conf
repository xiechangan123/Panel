# The site root goes straight to the front controller (nginx: index app.php)
@root path /
rewrite @root /app.php
# Route everything that isn't a real file to the front controller
@notfile {
	not file {
		try_files {path}
	}
}
rewrite @notfile /app.php{path}
